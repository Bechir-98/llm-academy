export const ragTopics = {
  'rag': { id: 'rag', title: 'RAG', description: 'Retrieval-Augmented Generation — giving LLMs access to external knowledge to reduce hallucinations.',
    sections: [
      { type: 'text', title: 'Why RAG?', icon: '📚', content: 'LLMs have a knowledge cutoff and hallucinate. **RAG** fixes this by retrieving relevant documents from your data and injecting them into the prompt. The model generates answers based on real information, not just its training data.\\n\\nThe flow: User asks question → Search your documents → Find relevant chunks → Add them to the prompt → LLM generates answer grounded in real data.' },
      { type: 'diagram', content: '  User Query → [Embedding Model] → Query Vector\n                                        ↓\n  Document Store → [Vector DB] → Similar Documents\n                                        ↓\n  Query + Documents → [LLM] → Grounded Answer', label: 'The RAG pipeline: retrieve, then generate' },
      { type: 'code', language: 'python', code: '# Simple RAG with LangChain\nfrom langchain.vectorstores import Chroma\nfrom langchain.embeddings import OpenAIEmbeddings\nfrom langchain.chat_models import ChatOpenAI\n\n# 1. Index documents\nvectorstore = Chroma.from_documents(docs, OpenAIEmbeddings())\n\n# 2. Retrieve relevant chunks\nretriever = vectorstore.as_retriever(search_kwargs={"k": 3})\nrelevant_docs = retriever.get_relevant_documents("What is our refund policy?")\n\n# 3. Generate with context\ncontext = "\\n".join([d.page_content for d in relevant_docs])\nprompt = f"Based on this context:\\n{context}\\n\\nAnswer: What is our refund policy?"' },
      { type: 'comparison', title: 'RAG vs Fine-Tuning', headers: ['Aspect', 'RAG', 'Fine-Tuning'], rows: [['When to use','Factual/up-to-date knowledge','Style/behavior changes'],['Data updates','Instant (update DB)','Requires retraining'],['Cost','Cheap (no training)','Expensive (GPU time)'],['Hallucination','Greatly reduced','Still possible']] }
    ],
    summary: ['RAG retrieves relevant docs and adds them to the prompt', 'Best for factual accuracy and up-to-date knowledge', 'Much cheaper and faster than fine-tuning', 'Dramatically reduces hallucinations'],
    mentalModel: 'An open-book exam — the model can look up answers instead of relying on memory.', mistakes: ['Not chunking documents properly', 'Retrieving too many or too few documents', 'Not using a reranker for better relevance', 'Expecting RAG to fix model reasoning — it helps with facts, not logic'],
    exercise: { description: 'Build a simple RAG system.', steps: ['Collect 5-10 documents about a topic', 'Chunk and embed them into a vector store', 'Build a retrieval pipeline', 'Test with questions that require document knowledge'] }
  },
  'vector-databases': { id: 'vector-databases', title: 'Vector Databases', description: 'Databases designed to store and search embeddings efficiently.',
    sections: [
      { type: 'text', title: 'Storing Meaning', icon: '🗄️', content: 'Regular databases search by exact matches (SQL WHERE name = "John"). **Vector databases** search by **similarity** — "find documents whose meaning is closest to this query." They store embeddings and use algorithms like HNSW or IVF for fast approximate nearest neighbor search.' },
      { type: 'comparison', title: 'Popular Options', headers: ['Database', 'Type', 'Best For'], rows: [['Chroma','Open-source, local','Prototyping, small projects'],['FAISS','Library (Facebook)','Fast similarity search, research'],['Pinecone','Cloud managed','Production, no infrastructure'],['Weaviate','Open-source','Hybrid search (vector + keyword)'],['Qdrant','Open-source','High performance, filtering']] },
      { type: 'code', language: 'python', code: '# ChromaDB example\nimport chromadb\n\nclient = chromadb.Client()\ncollection = client.create_collection("my_docs")\n\n# Add documents (auto-embeds)\ncollection.add(\n    documents=["AI is transforming healthcare", "Machine learning predicts weather"],\n    ids=["doc1", "doc2"]\n)\n\n# Query by meaning\nresults = collection.query(query_texts=["medical AI applications"], n_results=1)\n# Returns: "AI is transforming healthcare"' }
    ],
    summary: ['Store embeddings for similarity-based search', 'Search by meaning, not keywords', 'HNSW is the most common indexing algorithm', 'Chroma for prototyping, Pinecone for production'],
    mentalModel: 'A library organized by meaning instead of alphabetically — books about similar topics sit together.', mistakes: ['Using a regular database for embedding search', 'Not choosing the right distance metric', 'Storing too many or too few metadata fields', 'Not testing retrieval quality before deploying'],
    exercise: { description: 'Build a vector store.', steps: ['Install ChromaDB', 'Embed and store 20 text passages', 'Query with different questions', 'Evaluate if the top results are actually relevant'] }
  },
  'chunking': { id: 'chunking', title: 'Chunking', description: 'Splitting documents into smaller pieces for effective RAG retrieval.',
    sections: [
      { type: 'text', title: 'Why Chunk?', icon: '✂️', content: 'You can\'t embed an entire 100-page PDF as one vector — it would lose all specific meaning. **Chunking** splits documents into smaller pieces (typically 200-1000 tokens) that each capture a focused topic. These chunks are individually embedded and retrieved.' },
      { type: 'comparison', title: 'Chunking Strategies', headers: ['Strategy', 'How It Works', 'Best For'], rows: [['Fixed-size','Split every N characters/tokens','Simple, predictable'],['Recursive','Split by paragraphs → sentences → words','Structured documents'],['Semantic','Split when meaning changes','Research papers, varied content'],['Sentence-based','Split at sentence boundaries','Conversational text']] },
      { type: 'key-point', title: 'Overlap Matters', content: 'Always use **chunk overlap** (10-20% of chunk size). Without overlap, a relevant sentence at a chunk boundary gets split across two chunks, and neither chunk captures the full meaning.' }
    ],
    summary: ['Split documents into focused 200-1000 token chunks', 'Use overlap to avoid losing boundary information', 'Recursive splitting works well for most documents', 'Chunk size affects retrieval quality — experiment to find optimal'],
    mentalModel: 'Cutting a book into index cards — each card should cover one coherent point.', mistakes: ['Chunks too large (lose specificity) or too small (lose context)', 'No overlap between chunks', 'Ignoring document structure (headers, sections)', 'Same chunk size for all document types'],
    exercise: { description: 'Experiment with chunking.', steps: ['Take a long document', 'Chunk with different sizes: 200, 500, 1000 tokens', 'Embed and query each version', 'Compare which chunk size gives the best retrieval'] }
  },
  'retrieval-pipelines': { id: 'retrieval-pipelines', title: 'Retrieval Pipelines', description: 'End-to-end RAG flow from query to answer with reranking and hybrid search.',
    sections: [
      { type: 'text', title: 'Beyond Basic RAG', icon: '🔧', content: 'Basic RAG (embed → search → generate) works but has limitations. **Advanced retrieval pipelines** add: **query expansion** (rewrite queries for better matching), **hybrid search** (combine vector + keyword search), **reranking** (re-score results with a cross-encoder), and **answer synthesis** (cite sources, handle conflicting info).' },
      { type: 'diagram', content: '  Query → [Expand/Rewrite] → Enhanced Query\n                                    ↓\n  [Vector Search] + [BM25 Keyword] → Candidates\n                                    ↓\n  [Reranker (Cross-Encoder)] → Top Results\n                                    ↓\n  [LLM + Context] → Final Answer with Citations', label: 'Advanced retrieval pipeline with hybrid search and reranking' },
      { type: 'key-point', title: 'Reranking', content: 'Initial retrieval is fast but imprecise. A **reranker** (like Cohere Rerank or a cross-encoder model) takes the top 20-50 results and re-scores them more carefully, selecting the truly best 3-5. This significantly improves answer quality.' }
    ],
    summary: ['Advanced pipelines add query expansion, hybrid search, and reranking', 'Hybrid search combines vector similarity with keyword matching', 'Reranking significantly improves retrieval precision', 'The best RAG systems combine multiple retrieval strategies'],
    mentalModel: 'Google Search: initial broad search (retrieval), then ranking by relevance (reranking), then showing the best results.', mistakes: ['Skipping reranking — it\'s one of the biggest quality improvements', 'Only using vector search (missing exact keyword matches)', 'Not evaluating retrieval quality separately from generation', 'Too many or too few retrieved documents in context'],
    exercise: { description: 'Build an advanced pipeline.', steps: ['Start with basic RAG', 'Add BM25 keyword search alongside vector search', 'Add a reranker (use Cohere or a cross-encoder)', 'Compare answer quality before and after each addition'] }
  },
  'ai-memory-systems': { id: 'ai-memory-systems', title: 'AI Memory Systems', description: 'How AI applications remember past conversations and user preferences.',
    sections: [
      { type: 'text', title: 'Types of AI Memory', icon: '🧠', content: 'LLMs are stateless — each API call starts fresh with no memory. Applications build memory on top:\\n\\n**Buffer Memory**: Store last N messages in conversation history (simple, limited)\\n**Summary Memory**: LLM summarizes old conversations, stores the summary\\n**Vector Memory**: Embed past messages, retrieve relevant ones via similarity\\n**Entity Memory**: Track facts about entities (people, projects) mentioned in conversations' },
      { type: 'analogy', title: 'Human Memory', content: 'Buffer = short-term memory (what someone just said). Summary = long-term memory (general knowledge of a person). Vector = associative memory (a smell triggers a specific memory). Entity = semantic memory (knowing facts about people).' },
      { type: 'key-point', title: 'MemGPT / Letta', content: 'The **MemGPT** approach gives LLMs a virtual "operating system" for memory: a working context (like RAM) and long-term storage (like a hard drive). The LLM learns to manage its own memory — deciding what to save, retrieve, or forget.' }
    ],
    summary: ['LLMs are stateless — memory must be built by the application', 'Types: buffer, summary, vector, entity memory', 'Vector memory enables semantic retrieval of past conversations', 'MemGPT/Letta represents the cutting edge of AI memory'],
    mentalModel: 'AI memory is like building a filing system for a person with amnesia — they need external tools to remember.', mistakes: ['Stuffing entire conversation history into context (exceeds limits)', 'Not summarizing old conversations', 'Using only buffer memory for long interactions', 'Not separating memory types for different needs'],
    exercise: { description: 'Build a memory system.', steps: ['Create a chatbot that stores conversation in a buffer', 'Add summary memory for old conversations', 'Add vector memory for semantic retrieval', 'Test: can the bot remember things from 50+ messages ago?'] }
  },
  'semantic-search': { id: 'semantic-search', title: 'Semantic Search', description: 'Searching by meaning instead of keywords.',
    sections: [
      { type: 'text', title: 'Beyond Keywords', icon: '🔎', content: 'Traditional search matches exact keywords: searching "car" won\'t find documents about "automobile." **Semantic search** uses embeddings to match by meaning: "car" and "automobile" would have similar embeddings and match. It understands synonyms, paraphrases, and conceptual similarity.' },
      { type: 'comparison', title: 'Keyword vs Semantic', headers: ['Aspect', 'Keyword (BM25)', 'Semantic'], rows: [['Query: "car"','Finds: "car"','Finds: "car", "automobile", "vehicle"'],['Synonyms','Misses them','Catches them'],['Typos','May miss','Often handles'],['Speed','Very fast','Slightly slower'],['Best for','Exact terms, names','Concepts, natural questions']] },
      { type: 'code', language: 'python', code: '# Semantic search implementation\nfrom sentence_transformers import SentenceTransformer\nimport numpy as np\n\nmodel = SentenceTransformer("all-MiniLM-L6-v2")\n\n# Embed documents\ndocs = ["How to fix a flat tire", "Car maintenance tips", "Making pasta"]\ndoc_embeddings = model.encode(docs)\n\n# Search by meaning\nquery = "vehicle repair"\nquery_emb = model.encode([query])\n\n# Cosine similarity\nscores = np.dot(doc_embeddings, query_emb.T).flatten()\nbest_match = docs[np.argmax(scores)]  # "How to fix a flat tire"' },
      { type: 'key-point', title: 'Hybrid Search', content: 'The best approach combines both: **semantic search** for conceptual matching + **BM25 keyword search** for exact terms. This catches both meaning-based matches and precise keyword matches.' }
    ],
    summary: ['Searches by meaning using embeddings, not just keywords', 'Handles synonyms, paraphrases, and conceptual queries', 'Hybrid search (semantic + keyword) gives best results', 'Foundation for RAG and modern search engines'],
    mentalModel: 'A librarian who understands your question vs. a search bar that only matches exact words.', mistakes: ['Using only semantic search (misses exact keyword matches)', 'Wrong embedding model for your domain', 'Not normalizing vectors before similarity computation', 'Not evaluating search quality with real user queries'],
    exercise: { description: 'Build semantic search.', steps: ['Install sentence-transformers', 'Embed a collection of 50+ sentences', 'Search with natural language queries', 'Compare results with simple keyword matching'] }
  }
};
